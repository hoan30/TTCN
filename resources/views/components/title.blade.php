<div class="p-body-header">
    <div class="p-title ">
        <h1 class="p-title-value">
            @if (isset($title))
                {{ $title }}
            @else
                {{ 'Phuot.vn - Nơi Phượt bắt đầu' }}
            @endif
        </h1>
    </div>
</div>
